{
  "$id": "https://surface-concept.com/schema/epicsparam",
  "title": "EPICS properties for control parameter",
  "description": "EPICS (epics-controls.org) specific properties",
  "type": "object",
  "properties": {
    "asynportname": {
      "description" : "asynportdriver parameter name used in db records (INP/OUT)",
      "type" : "string"
    },
    "address" : {
      "description" : "only for 2D arrays, the address in the areaDetector driver",
      "type" : "integer",
      "minimum" : 0
    }
  },
  "required": [ "asynportname" ]
}
